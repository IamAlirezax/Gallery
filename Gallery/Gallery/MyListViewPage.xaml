<?xml version="1.0" encoding="utf-8"?>





<ContentPage
    Padding="0,20,0,0"
    xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Gallery.MyListViewPage">
    <StackLayout>

   <SearchBar Placeholder="Search..." TextChanged= "Handle_TextChanged" />
   <ListView x:Name="MyListView" ItemSelected="">
       <ListView.ItemTemplate>
 <DataTemplate>
     
     <ViewCell>
         <StackLayout Orientation="Horizontal" Padding="5" >

             <Image Source="{ Binding ImageUrl  }" />
             <StackLayout HorizontalOptions="StartAndExpand">
                <Label Text= "{ Binding Name }" />
                 <Label Text= "{ Binding place }" />
                 <Label Text= "{ Binding Date }" />
                 <Label Text= "{ Binding Gallery }" />

             </StackLayout>

             <Button Text="Detail" />

         </StackLayout>
     </ViewCell>
 </DataTemplate>

       </ListView.ItemTemplate>
       </ListView>
  </StackLayout>
    
</ContentPage>
